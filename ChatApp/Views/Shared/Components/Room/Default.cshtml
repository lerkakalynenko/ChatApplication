@model IEnumerable<ChatApp.DAL.Entities.Chat>

    <div class="side-menu">

        @foreach (var chat in Model)
        {
            <a class="room-button" asp-controller="Home" 
               asp-action="Chat" asp-route-id="@chat.Id">
                @chat.Name
            </a>
        }
        
        @*<div class="modal active" id="create-room-modal">
         
            <form asp-controller="Home" asp-action="CreateRoom" class="modal-body">
                <button type="button" class="close" onclick="closeModal()">X</button>    
                <header>
                    Create Room
                </header>
                <div>
                    <input name="name"/>
                </div>
                <footer>
                    <button>Create Room</button>
                </footer>

            </form>
        </div>*@
        <a class="room-button" id="create-room-btn" href="#">
            <strong>+</strong>
        </a>
        
    </div>