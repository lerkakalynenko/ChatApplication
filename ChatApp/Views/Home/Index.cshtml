@model IEnumerable<ChatApp.DAL.Entities.Chat>
@{
    ViewData["Title"] = "Home Page";
}


<div class="chat-body">
    @foreach(var chat in Model) {
        <a class="room-button" asp-controller="Chat" asp-action="JoinRoom" asp-route-id="@chat.Id">
            @chat.Name
        </a>
    }
</div>

